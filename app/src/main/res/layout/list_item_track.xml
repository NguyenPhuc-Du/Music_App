<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <ImageView
        android:id="@+id/logo_track"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_logo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:id="@+id/songName_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Song title"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:textSize="16sp"
        app:layout_constraintStart_toEndOf="@id/logo_track"
        app:layout_constraintTop_toTopOf="@id/logo_track"
        app:layout_constraintEnd_toStartOf="@id/approve_track_btn"
        android:layout_marginStart="12dp" />

    <TextView
        android:id="@+id/singerName_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Artist"
        android:textColor="#666"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="@id/songName_title"
        app:layout_constraintTop_toBottomOf="@id/songName_title"
        app:layout_constraintEnd_toStartOf="@id/approve_track_btn"
        android:layout_marginTop="4dp" />

    <TextView
        android:id="@+id/status_song"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pending"
        android:textSize="12sp"
        android:textColor="#FF5722"
        app:layout_constraintStart_toStartOf="@id/songName_title"
        app:layout_constraintTop_toBottomOf="@id/singerName_title"
        android:layout_marginTop="6dp" />

    <Button
        android:id="@+id/approve_track_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Approve"
        android:textAllCaps="false"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toStartOf="@id/delete_track_btn"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <Button
        android:id="@+id/delete_track_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Delete"
        android:textAllCaps="false"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
